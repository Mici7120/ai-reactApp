const connectedHex = require("./connectedHex.js");
const playerHex = require("./playerHexs.js");

/**
 * 
 * @param {*} board 
 */
function getMoves(board){
    let hexs = playerHex(board);

    return moves;
}

module.exports = getMoves;